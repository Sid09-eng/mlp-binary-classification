# Binary Classification using Multi-Layer Perceptron (PyTorch)

This project implements a neural network in PyTorch for binary classification.
Features:
- Data preprocessing
- MLP model training & evaluation
- Validation accuracy: ~88%
